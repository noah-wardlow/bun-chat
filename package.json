{
  "name": "server",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch ./src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./build --target bun"
  },
  "dependencies": {
    "@newbrains/prizma": "^1.0.3",
    "ioredis": "^5.3.2",
    "jsonwebtoken": "8.5.1"
  },
  "devDependencies": {
    "@types/ioredis": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.3",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}